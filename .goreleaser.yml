builds:
  - binary: faas-natsd
    ldflags: -s -w -X main.version={{.Version}}
    goos:
      - linux
    goarch:
      - amd64
      - arm
    goarm:
      - 7
    env:
      - CGO_ENABLED=0

dockers:
  - 
    binary: faas-natsd
    image: quay.io/nicholasjackson/faas-nats
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile
    latest: true
    tag_template: "{{ .Tag }}"
  - 
    binary: faas-natsd
    image: quay.io/nicholasjackson/faas-nats
    goos: linux
    goarch: arm
    goarm: 7
    dockerfile: Dockerfile_arm7
    tag_template: "arm7-{{ .Tag }}"
